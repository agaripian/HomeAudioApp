<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Disabled state -->
    <item android:color="@color/slider_thumb_dark_disabled" android:state_enabled="false" />
    <!-- Enabled state -->
    <item android:color="@color/slider_thumb_dark" android:state_enabled="true" />
    <!-- Default state (should not be needed, but including as fallback) -->
    <item android:color="@color/slider_thumb_dark" />
</selector> 